import React from 'react';
import ReactDOM from 'react-dom' //renders app to screen(dom)
import App from './App.js' 

ReactDOM.render(
  <App />, document.getElementById('root')
) 
//connecting app to id root

